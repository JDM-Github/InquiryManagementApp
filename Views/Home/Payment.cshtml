@model InquiryManagementApp.Models.Enrollment

@{
    ViewData["Title"] = "Payment Page";
}
@* 
<div class="container mt-5" style="min-height: 100vh !important;">
    <h2 class="text-center">Payment Page</h2>
    <hr />

    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h4>Student Information</h4>
        </div>
        <div class="card-body">
            <p><strong>Name:</strong> @Model.Firstname @Model.Surname</p>
            <p><strong>Grade Level:</strong> @Model.GradeLevel</p>
            <p><strong>LRN:</strong> @Model.LRN</p>
        </div>
    </div>

    <div class="mt-4">
        <h4>Payment Details</h4>
        <table class="table table-bordered">
            <thead class="bg-primary text-white">
                <tr>
                    <th>Description</th>
                    <th>Amount</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Tuition Fee</td>
                    <td>&#8369; @Model.FeePaid</td>
                </tr>
                <tr>
                    <td>Outstanding Balance</td>
                    <td>&#8369; @(5000 - Model.FeePaid)</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="mt-4">
        <h4>Make a Payment</h4>
        <form id="paymentForm" asp-controller="Payment" asp-action="ProcessPayment" method="post">
            <div class="mb-3">
                <label for="amount" class="form-label">Enter Payment Amount</label>
                <input type="number" name="amount" id="amount" class="form-control" min="1"
                    max="@(5000 - Model.FeePaid)" required />
            </div>
            <button type="submit" class="btn btn-primary">Pay Now</button>
        </form>
    </div>

    <div class="mt-4">
        <h4>Payment History</h4>
        @if (Model.PaymentHistory != null && Model.PaymentHistory.Count > 0)
        {
            <ul class="list-group">
                @foreach (var payment in Model.PaymentHistory)
                {
                    <li class="list-group-item">
                        <strong>Date:</strong> @payment.Date.ToString("g") |
                        <strong>Amount:</strong> &#8369; @payment.Amount
                    </li>
                }
            </ul>
        }
        else
        {
            <div class="alert alert-warning">No payment history available.</div>
        }
    </div>
</div> *@
